/**
 * @yuan/mcp-server — Public API
 *
 * Re-exports for programmatic usage (embedding the MCP server).
 */

export { registerToolsOnServer } from "./tools-adapter.js";
export { registerResources } from "./resources.js";
