{
  "name": "@yuan/mcp-server",
  "version": "0.1.0",
  "description": "YUAN Coding Agent MCP Server",
  "license": "AGPL-3.0",
  "type": "module",
  "bin": { "yuan-mcp": "dist/server.js" },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && chmod +x dist/server.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@yuan/core": "workspace:*",
    "@yuan/tools": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
