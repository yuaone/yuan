{
  "name": "yuan-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "YUAN — Autonomous Coding Agent (Open Core)",
  "license": "AGPL-3.0",
  "packageManager": "pnpm@10.26.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @yuaone/core dev & pnpm --filter @yuaone/cli dev",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "pnpm": {
    "overrides": {
      "hono": ">=4.12.7",
      "@tootallnate/once": ">=3.0.1"
    },
    "onlyBuiltDependencies": ["node-pty"]
  }
}
